{
  "name": "liamhowell.com",
  "version": "2.0.0",
  "description": "My personal site.",
  "author": "Liam Howell",
  "license": "ISC",
  "homepage": "https://liamhowell.com",
  "scripts": {
    "dev": "npx serve src -p 3000",
    "deploy": "MSG=$(git log -1 --format=%s) && TARGET=$(cd \"$(git rev-parse --git-common-dir)/../..\" && pwd)/liamitus.github.io && cp -R src/* \"$TARGET\" && cd \"$TARGET\" && git add . && git commit -am \"Deploy: $MSG\" && git push"
  }
}
